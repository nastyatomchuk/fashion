<%= simple_form_for @rate do |form| %>
  <%= form.association :currency, label: "entity", collection: @currencies.order(:entity), label_method: :entity, prompt: "< Choose entity >" %>
  <%= form.input :rate, label: "rate" %>

  <%= form.button :submit, class: "bnt btn-primary bnt-lg" %>
<% end %>
