<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @product.title %>
</p>

<div>
  <%= image_tag(@product.image.url, alt: 'Image', :size => "250x350") if @product.image? %>
</div>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>


  <% @cate =  Category.find_by_sql("select * from categories inner join products on categories.id = products.category_id where categories.id = #{@product.category_id}%")%>

<p>
  <strong>category:</strong>
  <%= @cate %>
</p>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
